name: google_docs
type: knowledge
version: 1.0.0
label: "Google Docs"
group: personal
order: 25
default_enabled:
  private: true
requires_mode: private
requires_skills:
  - bash_exec
capability: "Create, read, edit, and export Google Docs"
description: |
  Google Docs (create/read/write/edit/export) via the gog CLI. Call
  load_skill('google_docs') for the gog command reference and the
  first-time setup protocol, then act via bash_exec.
